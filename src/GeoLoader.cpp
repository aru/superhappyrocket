#include "GeoLoader.h"

/* LoadFromTxt () -
 * Loads some geometry from a .txt file and loads
 * it into memory as our current Visual structure
 * recast to appropiate filetype when using.
*/
void GeoLoader::LoadFromTxt( char* FILE )
{
	//// Check pointer to null
	//assert( FILE );
	//
	//// check pointer to null
	//if( !FILE )
	//{
	//	// Log error
	//	std::cout << "File is not valid (pointer to NULL)";
	//	return;
	//}

	//// create our file struct
	//std::fstream file;

	//// open file ONLY for reading
	//file.open( FILE, std::fstream::in );

	//// read the file and put it into a Visual thing
	//

	//// close the file
	//file.close();
}